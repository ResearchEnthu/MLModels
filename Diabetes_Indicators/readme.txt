Explore the data :- 
--- df.info ():- Give the information about the dataset.
--- df.describe():- It describe the Count, mean, standard deviation, min, %25, %50 and %70 and max values.
--- df.duplicate().sum():- use to find the number of duplicates in dataset.

Data Cleaning :- 

--- Drop the unnecesarry features --> df.drop(['Income', 'Education']) these features are not related to problem statement.
--- # Converting datatypes of some features

Data Visualization ":- 


Data Preprocessing :- 
---df.columns[1:] means "select all columns starting from index 1 onwards" — that is, it skips the first column (index 0).
